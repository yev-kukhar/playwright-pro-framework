# Copy to .env for local runs:
#   cp .env.example .env

# PrivatBank API Base URL
API_BASE_URL=https://api.privatbank.ua

# SauceDemo Base URL
BASE_URL=https://www.saucedemo.com
UI_USERNAME=standard_user
UI_PASSWORD=secret_sauce

# Reporter Configuration
# Options: playwright | allure
REPORTER=playwright

# Test Environment overlay selector
# Options: local | stage | ci | production
TEST_ENV=local

# Test data
DEFAULT_CURRENCY=USD
TEST_DATE=01.12.2023
